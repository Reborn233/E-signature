# 电子签名应用原型-react


```
# install package
npm install

进入 node_modules/react-scripts/config/env.js 修改一下代码
// const NODE_ENV = process.env.NODE_ENV;
const NODE_ENV = process.env.FS_ENV;

# preview
npm run dev


# build
npm run build
```